function showMessage(texto: string): string {
  return texto;
}

function showMessage2(texto: string, texto2: string): string {
  return texto + texto2;
}

const arrow = (soma: number): number => soma + 1;

function anyReturns(valor: string | number): string | number {
  return valor;
}

