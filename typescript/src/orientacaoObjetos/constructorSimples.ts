class ConstructorSimples {
  constructor(
    private nome: string,
    private senha: string,
    private idade: number
  ) {}
}

const construcutorSimples = new ConstructorSimples("nome", "senha", 10);
